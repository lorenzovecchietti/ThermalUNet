#!/bin/sh
cd "${0%/*}" || exit                                # Run from this directory
. $WM_PROJECT_DIR/bin/tools/RunFunctions
#------------------------------------------------------------------------------

runApplication gmshToFoam circuit_mesh.msh
runApplication changeDictionary

runApplication splitMeshRegions -cellZones -overwrite

#------------------------------------------------------------------------------
